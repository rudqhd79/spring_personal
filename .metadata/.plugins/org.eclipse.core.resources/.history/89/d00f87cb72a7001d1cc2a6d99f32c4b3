package com.busreservation.repository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.querydsl.QuerydslPredicateExecutor;

import com.busreservation.entity.Path;
import com.busreservation.entity.Terminal;

public interface PathRepository extends JpaRepository<Path, Long>  {
	
	
//	@Query(value="select count(*) from userinfo where email = :email and user_pw = :user_pw", nativeQuery=true)
//	List<Terminal>
	//select * from path where path_start =? && path_end =?
//	List<Path> findByPath(String start, String end);

}
