package com.busreservation.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.querydsl.QuerydslPredicateExecutor;

import com.busreservation.entity.Path;

public interface PathRepository extends JpaRepository<Path, Long>, QuerydslPredicateExecutor<Path>, PathRepositoryCustom  {
	
	//select * from path where path_start =? && path_end =?
	List<Path> findByPath

}
